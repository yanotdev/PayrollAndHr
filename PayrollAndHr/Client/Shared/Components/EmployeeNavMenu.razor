@using PayrollAndHr.Client.Pages;

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">PayrollAndHr</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="dashboard/overview" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3 text-white" @onclick ="ToggleVisibility">            
                <i class="fa fa-cog"></i><span class="oi" >Employee</span>
                <div class="arrow pull-right"><i class="fa fa-angle-down"></i></div>
            @if(IsVisible){
                <ul id="setupList" class="collapse list-unstyled">
                    <li><button @onclick="ShowMyModal">Staff Number Prefix</button></li>
                    <li> <NavLink href="/registeremployee">Register Employee</NavLink></li>
                    <li> <NavLink href="/employeesearch">Employee Search</NavLink></li>
                        <li> <a href="#">Staff Status</a></li>
                        <li> <a href="#">Document Upload</a></li>
                    </ul>                
            }
           
        </div>
       
    </nav>
</div>


<EmployeeStaffNoPrefix Title="My Modal" OnClose="CloseModal">
    <form>

        <div class="row">
            <div class="col-md-3">
                <label class="custom-control-description">Prefix</label>
            </div>
            <div class="col-md-9">
                <input type="text" name="prefix" class="form-control" id="txtPrefix" />
            </div>
        </div>
    </form>
</EmployeeStaffNoPrefix>

@code {
    private EmployeeStaffNoPrefix? _modalRef;

    private void ShowMyModal()
    {
        _modalRef?.ShowModal();
    }

    private void CloseModal()
    {
        
        // Handle modal close event if needed
    }
}

@code {
    private bool collapseNavMenu = true;
    private bool IsVisible = false;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private void ToggleVisibility()
    {
        IsVisible = !IsVisible;
    }
}


